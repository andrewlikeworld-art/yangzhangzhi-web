# 复制成 .env.local 后填。两个都不填也能跑 —— 会走离线假客服 + mock 商品,
# 交互能完整演示,只是不接真 AI。

# cn-kefu 服务地址。生产:与本服务同处微信云托管环境,填**内网**地址
# (cn-kefu 的公网访问保持关闭,不要填公网域名)。
# 本地开发接真后端:先在 kefu 仓 `PORT=3500 pnpm dev`,这里填 http://127.0.0.1:3500
KEFU_API_BASE=

# cn-kefu 的 web 专用 Bearer key。只在服务端用,不会下发到浏览器。
# ⚠️ 填 web key,别填管理 key —— 管理 key 能导出全部顾客对话,不该出现在前台站;
# web key 权限只够商品/发消息/取回复(碰 /api/admin/* 返 403 是刻意的)。
KEFU_WEB_KEY=

# 旧变量,过渡兜底(没配 KEFU_WEB_KEY 时才生效)。部署侧换好新 key 后删掉。
KEFU_SHARED_KEY=
