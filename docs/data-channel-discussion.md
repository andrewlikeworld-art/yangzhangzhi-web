# web 站数据通道 · 讨论文档

> 活文档:每次讨论后就地更新,不记流水账。打开任何时刻应能看到「决策路径 + 当前状态」。
> 开始于 2026-08-04(首页按参考图改版后,数据需求集中爆发)。

## 背景速览

- web 站定位:**纯展示端,不配后台**。内容管理台统一在小程序端(nvzhuang-shop)。
- 现状数据链:浏览器 → web BFF → cn-kefu `/api/products` → 云开发库 `products`(60s 缓存,
  cloud:// 图换 2h 临时 URL)。商品的**自动同步今天就成立**(商家上传 → web 约 60s 可见)。
- 缺通道的数据:穿搭灵感、品类、颜色、商家尺码推荐表、无水印原图。
- 协作通道:`~/Documents/web-shop-中转站`(web↔小程序端信箱,2026-08-04 建)。

## 已定决策

- **2026-08-04 · 数据通道全部直连小程序端(方案 B)**
  商品/品类/颜色/尺码/穿搭灵感全走 shop 新开的只读接口;kefu 只管 AI 对话。
  理由:后台统一在 shop 的长期形态最干净;穿搭灵感是内容不是商品,不该让 kefu 变成
  内容网关;kefu 公网还是待更换的测试域名,且刚发生过缓存事故。
  放弃的选项:A(继续经 kefu 透传,信已撤回)、混合(商品经 kefu + 灵感直连,双上游)。
- **2026-08-04 · 品类以商家后台类目为准**,不用 AI 打标(打标继续服务 kefu 自己的推荐)。
  ⚠️ 待 shop 确认库里 category 字段的真实语义(疑似「杨哥私服/预售」这类运营分组)。
- **2026-08-04 · 穿搭灵感形态**:首页一个区块 + 点开半屏图文 + 可跳关联商品。不做独立页。
- **2026-08-04 · 同步时效分钟级**(web 侧 60s 缓存),不做推送。
- **2026-08-04 · 迁移不断档**:shop 接口上线前,web 照旧走 kefu `/api/products`。

## 未决问题

- **shop 接口的字段清单与地址形态** —— 卡在等 shop 回信
  (需求信:web-shop 信箱 `2026-08-04-请开只读数据接口-商品与穿搭灵感.md`)。
- **商家类目字段现状** —— 等 shop 说明;若后台没有正经品类管理,web 回退方案再议。
- **无水印原图** —— 素材问题非代码问题(水印烧在文件里)。卡在:用户/Andrew 问店铺
  有没有发小红书前的原图。有 → shop 加字段重传;无 → 继续用带水印图排版。
- **web 只读 key 的交付** —— 接口成型后走人对人渠道,绝不进信箱/文档/commit。
- **迁移后 kefu `/api/products` 的去留** —— 它将失去唯一消费方;已告知 kefu,由其自定。

## 下一步

1. 等 shop 回接口初版(字段清单 + 地址形态 + 图片 URL 口径)→ web 写对接层
   (只改 `lib/products.ts` 数据源 + 新增 `lib/looks.ts`,组件层不动)。
2. 用户问商家要原图的结论 → 回给 shop 信箱。
3. 接口切换后:删 `lib/catalog.ts` 的标题猜词逻辑,分类改吃商家类目;
   商品卡补色点行(过滤隐藏色);通知 kefu 关于 `/api/products`。
