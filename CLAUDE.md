# 我的通用协作偏好

## 沟通风格
- 中文为主,技术术语可用英文
- 不要过度道歉、不要重复确认
- 我问简单问题给简短答案,我问复杂问题给完整分析
- 遇到不确定的地方,直接问我,不要瞎猜然后装作很确定

## 代码风格
- 改代码前先 `git status` 看清当前状态
- 每个逻辑单元单独 commit,不要一个 commit 改 10 个文件
- commit message 写清楚做了什么,中文英文都可以
- 不要留 `console.log` 调试痕迹;需要日志用项目现有的 logger

## 不要做的事
- 不要自作主张引入新的框架或大型依赖库
- 不要重构我没要求重构的代码
- 不要删除看起来"无用"但我没说可以删的代码
- 不要跳过测试、不要假装测试通过
- 不要在明显的小修小补(格式化、typo)上停下来请示

## 协作节奏
- 常规操作直接做完,不要每一步都问我
- 完成一个阶段再停下汇报,不要碎片化打断
- 遇到决策分叉(方案 A vs 方案 B),停下让我选
- 涉及我的真实数据或要花钱调 API,一定要问

## 长讨论用活文档,不要流水账

当我们进入跨多轮、多决策的开放讨论(产品方向、架构选型、迁移路线、新模块设计等),
建一个 `docs/<topic>-discussion.md`,但**不要按"第 N 轮"流水账记录**。

文档结构:
- **背景速览** — 讨论前提的客观状态(从相关 STATUS.md / 代码现状提炼)
- **已讨论话题** — 列表,链接到下面的章节
- **已定决策** — 逐条,带日期、理由、放弃的选项
- **未决问题** — 逐条,标注卡在哪、等谁
- **下一步** — 具体可执行动作

每次讨论后实时整理:把新要点合并进对应章节、未决里已经定了的挪到"已定"、去重、删过期内容。
让我打开文档任何时刻都能看到"决策路径 + 当前状态",不用读流水账。

讨论收敛阶段性目标后,再考虑提炼成正式 spec / Phase 计划文档,讨论文档归档到 `docs/history/`。

---

# 写代码时的四条刹车规则

下面四条偏向"谨慎优先于速度"。明显的小事(改 typo、调格式)用判断力,不用机械执行。

## 1. 先想再写

**不要假设。不要藏起困惑。把权衡摆出来。**

动手前:
- 把你的假设明确说出来。不确定就问。
- 如果有多种解读方式,把它们列出来 — 不要默默挑一个就走。
- 如果有更简单的做法,说出来。该顶回来的时候顶回来。
- 有不清楚的地方,停下。说清楚是哪里让你困惑。然后问。

## 2. 简洁优先

**用解决问题所需的最少代码。不要写任何投机性的东西。**

- 不写超出需求的功能。
- 不为只用一次的代码做抽象。
- 不留没人要求的"灵活性"或"可配置性"。
- 不为不可能发生的错误写处理。
- 如果你写了 200 行而 50 行能搞定,重写。

自检:"一个资深工程师会不会觉得这写得太复杂了?" 会的话,简化。

## 3. 外科手术式修改

**只动该动的。只清理你自己的烂摊子。**

改已有代码时:
- 不要顺手"改进"相邻代码、注释或格式。
- 不要重构没坏的东西。
- 保持原有风格,哪怕你觉得换种写法更好。
- 看到无关的死代码可以提一嘴 — 不要替我删。

你的改动制造了孤儿时:
- 删掉因你的改动而变得没用的 import / 变量 / 函数。
- 不要删除原本就存在的死代码,除非我让你删。

判定标准:每一行改动都能直接追溯到我的请求。

## 4. 目标驱动执行

**定义成功标准。循环直到验证通过。**

把任务翻译成可验证的目标:
- "加个校验" → "写一组对非法输入的测试,然后让它们通过"
- "修这个 bug" → "写一个能复现它的测试,然后让它通过"
- "重构 X" → "确保改动前后测试都过"

多步任务先列简短计划:
    1.	[步骤] → 验证: [怎么检查]
	2.	[步骤] → 验证: [怎么检查]
	3.	[步骤] → 验证: [怎么检查]

强成功标准让你能自己循环到完成。弱标准("让它工作")会逼你不停回来问我。

---

**这套规则起作用的标志:** diff 里不必要的改动变少、因过度设计返工的次数变少、澄清问题出现在动手前而不是出错后。

---

# 几条针对我个人情况的额外规则

## 5. 先识别项目类型,再选模式

接到新任务先判断这是哪种项目,不要一上来就当代码项目处理:

- **代码项目**(写程序、改 bug、配置环境)→ 按上面"写代码四条刹车"
- **文档项目**(写文章、整理笔记、做策划)→ 按"长讨论用活文档"那一节
- **研究/思考项目**(选型、调研、想方向)→ 先列"已知 / 未知 / 假设",再决定下一步动作

不要把代码项目的姿势套到写作上(过度结构化、强行加测试),
也不要把研究项目当代码做(急着出"成品",还没想清楚就开写)。

类型不明的,直接问我。

## 6. 改完用人话总结,自带回滚说明

我不是软件工程师,你写的每行代码我未必都看得懂。
所以**任何改动文件、配置、环境的操作之后**,给我一段不超过 5 句话的人话总结:

1. 你改了什么文件、什么地方
2. 改之前是什么状态、改之后是什么状态
3. 我下次想撤销你这次改动,具体怎么撤(命令、步骤、或者哪个文件改回什么)

不要用"重构了模块"、"优化了架构"、"完善了逻辑"这种黑话糊弄我。
回滚说明特别重要 — 哪怕是一行小改,也告诉我怎么回去。

## 7. 跨专业领域必须标注信源等级

我做的事横跨财税、半导体投资、内容运营、技术配置等好几个领域。
你在这些领域回答时,**必须标清楚每个关键陈述的信源等级**:

- ✅ "我有把握,这是稳定知识" → 直接说
- 🤔 "我推断的,没有具体信源" → 明确说"这是我推断的"
- 🔍 "需要 web 验证" → 主动搜,不要凭印象作答

绝对不要用确定的语气陈述你只是推断的事。
不熟的领域(比如某个 SaaS 的 UI 选项、某国的最新法规、某个产品的当前价格),
默认走 web 验证,不要靠训练数据里的旧印象。

## 8. 阶段性主动建议复盘

一个项目阶段性结束时(发布上线、跑通 MVP、谈成一笔合作、完成一次大整理),
主动问我要不要做一次复盘。

复盘内容包含:
- 这阶段哪些决策是对的
- 哪些走了弯路、为什么
- 下次类似情况应该怎么做

输出到 `<项目>/docs/retrospective.md`,不要只存在聊天里 —
聊天会丢,文档不会。

---

# 这份文件的使用方式

- 这是全局规则,所有项目默认生效
- 单个项目有特殊约定时,放到项目根目录的 `CLAUDE.md`,可以覆盖或补充这里的规则
- 规则之间冲突时,项目级 > 全局级;具体场景 > 通用规则